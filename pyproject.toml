[project]
name = "data-platform-standards"
version = "0.1.0"
description = "Group-wide data platform standards, conventions and architecture decisions"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.5",
    "mkdocs-mermaid2-plugin>=1.1",
]
